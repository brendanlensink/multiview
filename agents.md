# Agents

Notes for AI agents working on this project.

## Xcode Project Structure

This project uses `PBXFileSystemSynchronizedRootGroup`, so Xcode auto-discovers files in the `MultiView/` directory. No `pbxproj` changes are needed when adding or removing source files.
